{
  "dependencies": {
    "ejs": "^3.1.7",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "browser-sync": "^2.24.5",
    "nodemon": "^2.0.16"
  },
  "scripts": {
    "dev": "nodemon server.js & npm run ui",
    "ui": "browser-sync start --proxy=localhost:8080 --files='**/*.css, **/*.ejs, **/*.js' --ignore=node_modules --reload-delay 10 --no-ui --no-notify"
  }
}
